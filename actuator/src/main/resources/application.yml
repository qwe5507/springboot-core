management:
  info:
    java:
      enabled: true
    os:
      enabled: true
    env:
      enabled: true
    git:
      mode: full
  endpoint:
    health:
#      show-details: always
      show-components: always
    shutdown:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
info:
    app:
      name: hello-actuator
      company: yh
server:
  port: 8081

logging:
  level:
    hello.controller: debug